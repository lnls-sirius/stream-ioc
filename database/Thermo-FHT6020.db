# Thermo-FHT6020.db

# Record definition file for one Thermo Fisher Scientific FHT 6020 controller.

# Each record defined below has the following parameters:
#
# PORT - Name of the port configured in asynDriver.
#
# PREFIX - Prefix name for all PVs associated with the device.

# Gamma

record(ai, "$(PREFIX):Gamma-T") {
    field(DESC, "Gamma")
    field(DTYP, "stream")
    field(EGU, "uSv/h")
    field(INP, "@Thermo-FHT6020.proto getGamma $(PORT)")
    field(PINI, "YES")
    field(PREC, "3")
    field(SCAN, "1 second")
    field(MDEL, "-1")
    field(FLNK, "$(PREFIX):Neutron-T")
}

# Neutron

record(ai, "$(PREFIX):Neutron-T") {
    field(DESC, "Neutron")
    field(DTYP, "stream")
    field(EGU, "uSv/h")
    field(INP, "@Thermo-FHT6020.proto getNeutron $(PORT)")
    field(PREC, "3")
    field(MDEL, "-1")
    field(FLNK, "$(PREFIX):TotalDoseRate-T")
}

# Total dose rate

record(calc, "$(PREFIX):TotalDoseRate-T") {
    field(DESC, "Total dose rate")
    field(EGU, "uSv/h")
    field(INPA, "$(PREFIX):Gamma-T NPP MSS")
    field(INPB, "$(PREFIX):Neutron-T NPP MSS")
    field(CALC, "A + B")
    field(PREC, "3")
    field(MDEL, "-1")
}
